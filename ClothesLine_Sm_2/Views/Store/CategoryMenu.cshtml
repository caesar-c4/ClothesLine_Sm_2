@model IEnumerable<ClothesLine_Sm_2.Models.Category>




        @foreach (var category in Model)
        {

            <p class="btn">
                @Html.ActionLink(category.Name, "Browse", "Store", new { Category = category.Name }, null)

                </p>

         }

